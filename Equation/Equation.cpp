#include <iostream>
#include <fstream>
#include <vector>
#include <cctype>
using namespace std;

int main()
{
    freopen("input.txt","r",stdin);
    string str;
    int den;
    int num;
    int flag=0;
    while(getline(cin,str))
    {
        flag=0;
        den=0;
        num=1;
        int len=str.length();
        while(1)
        {

        }
        cout<<str<<endl;
    }
}
